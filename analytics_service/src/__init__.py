"""Top-level package for analytics_service.

This file exists to ensure imports like `src.api.*` resolve reliably when the service
is executed by different runners (uvicorn, fastapi-cli, pytest, etc.).
"""
